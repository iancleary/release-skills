# Release workflow: https://github.com/iancleary/release-skills
[release]
runner_protocol = "prepared-v1"
name = "example-tag-only-project"
runner = [
  "uv", "run", "scripts/release.py", "tag-release",
  "--provider", "github",
  "--tag-prefix", "",
  "--branch", "main",
]
dry_run_args = ["--dry-run"]
publish = true

[checks]
commands = [
  ["./scripts/check.sh"],
]
